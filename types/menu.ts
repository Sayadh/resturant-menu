// Canonical menu types live in ~/data/menu.ts (co-located with the data).
// Re-exported here for convenience / external imports.
export type {
  Lang,
  BadgeKey,
  MenuLevelId,
  DrinkGroup,
  LocalizedText,
  MenuItem,
  MenuCategory,
  MenuLevel,
  DrinkGroupInfo,
} from '~/data/menu'
